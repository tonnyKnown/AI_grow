<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.mapper.DashboardMapper">

    <select id="getDashboardStats" resultType="java.util.HashMap">
        SELECT
            (SELECT COUNT(*) FROM sys_user) AS userCount,
            (SELECT COUNT(*) FROM sys_product) AS productCount,
            (SELECT COUNT(*) FROM sys_role) AS roleCount,
            (SELECT COUNT(*) FROM sys_permission) AS permissionCount
    </select>

</mapper>
